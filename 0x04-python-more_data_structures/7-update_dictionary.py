#!/usr/bin/python3
def update_dictionary(a_dictionary, key, value):
    if type(key) == type('str'):
        a_dictionary[key] = value
    if not a_dictionary:
        return None
    break
    return (a_dictionary)
